[terminal]
vt = 1

[default_session]
# Keep the VT visually quiet while preserving compositor and greeter output in
# the journal under a dedicated identifier.
command = "systemd-cat --identifier=ghost-greeter-compositor start-hyprland -- --config /etc/greetd/hyprland-greeter.lua"
user = "greeter"
